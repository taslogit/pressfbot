# 🧪 План тестирования системы друзей (Phase 4.1 MVP)

**Дата:** 2026-02-16  
**Версия:** 1.0

---

## 📋 Подготовка к тестированию

### 1. Проверка миграций БД

```bash
# Убедитесь что миграции применены
# Таблица friendships должна существовать
docker exec -it pressf-db-1 psql -U pressf_user -d pressf_db -c "\d friendships"
```

**Ожидаемый результат:**
- Таблица `friendships` существует
- Есть поля: `id`, `user_id`, `friend_id`, `status`, `requested_by`, `created_at`, `accepted_at`
- Есть UNIQUE constraint на `(user_id, friend_id)`
- Есть индексы

---

## 🧪 Тестовые сценарии

### Тест 1: Отправка запроса дружбы

**Шаги:**
1. Открыть приложение от имени User A
2. Перейти в раздел "Друзья" → вкладка "Поиск"
3. Найти User B (по username или ID)
4. Нажать "Добавить в друзья"

**Ожидаемый результат:**
- ✅ Toast уведомление: "Friend request sent!"
- ✅ В вкладке "Запросы" → "Исходящие запросы" появляется User B
- ✅ У User B в "Входящие запросы" появляется User A
- ✅ В поиске у User B статус меняется на "Запрос отправлен"

**Проверка в БД:**
```sql
SELECT * FROM friendships WHERE user_id = <UserA_id> AND friend_id = <UserB_id>;
-- Должна быть запись со status='pending', requested_by=UserA_id
```

---

### Тест 2: Принятие запроса дружбы

**Шаги:**
1. От имени User B открыть раздел "Друзья" → вкладка "Запросы"
2. В секции "Входящие запросы" найти User A
3. Нажать кнопку "Принять" (зеленая галочка)

**Ожидаемый результат:**
- ✅ Toast уведомление: "Friend request accepted!"
- ✅ User A исчезает из "Входящие запросы" User B
- ✅ User B появляется в "Мои друзья" User A
- ✅ User A появляется в "Мои друзья" User B
- ✅ В ленте активности обоих пользователей появляется событие "X добавил Y в друзья"

**Проверка в БД:**
```sql
-- Должны быть ДВЕ записи со status='accepted'
SELECT * FROM friendships 
WHERE (user_id = <UserA_id> AND friend_id = <UserB_id>) 
   OR (user_id = <UserB_id> AND friend_id = <UserA_id>);
-- Обе записи должны иметь status='accepted' и accepted_at IS NOT NULL
```

**Проверка активности:**
```sql
SELECT * FROM activity_feed 
WHERE activity_type = 'friend_added' 
AND (user_id = <UserA_id> OR user_id = <UserB_id>)
ORDER BY created_at DESC LIMIT 2;
-- Должны быть 2 записи (по одной для каждого пользователя)
```

---

### Тест 3: Отклонение запроса дружбы

**Шаги:**
1. User A отправляет запрос User C
2. От имени User C открыть "Друзья" → "Запросы"
3. В "Входящие запросы" найти User A
4. Нажать кнопку "Отклонить" (красный крестик)

**Ожидаемый результат:**
- ✅ Toast уведомление: "Request declined"
- ✅ User A исчезает из "Входящие запросы" User C
- ✅ User C исчезает из "Исходящие запросы" User A
- ✅ В поиске можно снова отправить запрос

**Проверка в БД:**
```sql
SELECT * FROM friendships 
WHERE (user_id = <UserA_id> AND friend_id = <UserC_id>) 
   OR (user_id = <UserC_id> AND friend_id = <UserA_id>);
-- Записей не должно быть (удалены)
```

---

### Тест 4: Удаление друга

**Шаги:**
1. User A и User B уже друзья
2. От имени User A открыть "Друзья" → "Мои друзья"
3. Найти User B в списке
4. Нажать кнопку "Удалить" (красный крестик)

**Ожидаемый результат:**
- ✅ Toast уведомление: "Friend removed"
- ✅ User B исчезает из "Мои друзья" User A
- ✅ User A исчезает из "Мои друзья" User B
- ✅ Можно снова отправить запрос дружбы

**Проверка в БД:**
```sql
SELECT * FROM friendships 
WHERE (user_id = <UserA_id> AND friend_id = <UserB_id>) 
   OR (user_id = <UserB_id> AND friend_id = <UserA_id>);
-- Записей не должно быть (удалены обе)
```

---

### Тест 5: Поиск пользователей

**Шаги:**
1. Открыть "Друзья" → "Поиск"
2. Ввести минимум 2 символа (например, часть username)
3. Проверить результаты

**Ожидаемый результат:**
- ✅ Появляются результаты поиска
- ✅ Показывается правильный статус:
  - "Уже друзья" (если `isFriend === true`)
  - "Запрос отправлен" (если `hasPending === true`)
  - Кнопка "Добавить" (если нет связи)
- ✅ Поиск работает по username и по user_id

---

### Тест 6: Рекомендации друзей

**Шаги:**
1. У User A есть рефералы (User B, User C)
2. User A и User B уже друзья
3. Открыть "Друзья" → вкладка "Рекомендации"

**Ожидаемый результат:**
- ✅ Показывается User C (реферал, но не друг)
- ✅ НЕ показывается User B (уже друг)
- ✅ Можно добавить User C в друзья

---

### Тест 7: Лента активности друзей

**Шаги:**
1. User A и User B друзья
2. User B выполняет действие (check-in, создает письмо, выигрывает дуэль)
3. User A открывает главную страницу → вкладка "Friends" в ActivityFeed

**Ожидаемый результат:**
- ✅ В ленте "Friends" показывается активность User B
- ✅ В ленте "All" также показывается активность User B
- ✅ Показывается событие "User B added User C as a friend" (если User B добавил друга)

---

### Тест 8: Edge Cases

#### 8.1. Попытка добавить себя в друзья
**Шаги:** User A пытается найти себя и добавить в друзья  
**Ожидаемый результат:** ❌ Ошибка "Cannot add yourself as friend"

#### 8.2. Двойная отправка запроса
**Шаги:** User A отправляет запрос User B дважды подряд  
**Ожидаемый результат:** ❌ Второй запрос отклоняется с ошибкой "Request already pending"

#### 8.3. Принятие несуществующего запроса
**Шаги:** User A пытается принять запрос от User C, который не отправлял запрос  
**Ожидаемый результат:** ❌ Ошибка "No pending request from this user"

#### 8.4. Поиск несуществующего пользователя
**Шаги:** Поиск по несуществующему username  
**Ожидаемый результат:** ✅ Пустой список результатов

#### 8.5. Поиск с менее 2 символов
**Шаги:** Поиск по 1 символу  
**Ожидаемый результат:** ✅ Пустой список (минимум 2 символа)

---

## 🔍 Проверка API напрямую (опционально)

### Тест через curl/Postman

**1. Получить список друзей:**
```bash
curl -X GET "https://your-domain.com/api/friends?limit=50&offset=0" \
  -H "X-Session-Id: YOUR_SESSION_ID"
```

**2. Получить pending запросы:**
```bash
curl -X GET "https://your-domain.com/api/friends/pending" \
  -H "X-Session-Id: YOUR_SESSION_ID"
```

**3. Отправить запрос дружбы:**
```bash
curl -X POST "https://your-domain.com/api/friends/request/123456" \
  -H "X-Session-Id: YOUR_SESSION_ID"
```

**4. Принять запрос:**
```bash
curl -X POST "https://your-domain.com/api/friends/accept/123456" \
  -H "X-Session-Id: YOUR_SESSION_ID"
```

**5. Поиск пользователей:**
```bash
curl -X GET "https://your-domain.com/api/friends/search?q=username&limit=20" \
  -H "X-Session-Id: YOUR_SESSION_ID"
```

**6. Рекомендации:**
```bash
curl -X GET "https://your-domain.com/api/friends/suggestions?limit=20" \
  -H "X-Session-Id: YOUR_SESSION_ID"
```

---

## 📊 Чеклист тестирования

### Backend API
- [ ] GET `/api/friends` возвращает список друзей
- [ ] GET `/api/friends/pending` возвращает входящие/исходящие запросы
- [ ] POST `/api/friends/request/:userId` создает запрос
- [ ] POST `/api/friends/accept/:userId` создает двустороннюю связь
- [ ] POST `/api/friends/decline/:userId` удаляет запрос
- [ ] DELETE `/api/friends/:userId` удаляет дружбу
- [ ] GET `/api/friends/search` находит пользователей
- [ ] GET `/api/friends/suggestions` возвращает рекомендации

### Frontend UI
- [ ] Экран "Друзья" открывается без ошибок
- [ ] Вкладка "Мои друзья" показывает список
- [ ] Вкладка "Запросы" разделяет входящие/исходящие
- [ ] Вкладка "Поиск" работает корректно
- [ ] Вкладка "Рекомендации" показывает предложения
- [ ] Кнопки "Принять/Отклонить" работают
- [ ] Кнопка "Удалить друга" работает
- [ ] Toast уведомления показываются
- [ ] Состояния загрузки отображаются

### Интеграция
- [ ] ActivityFeed показывает активность друзей
- [ ] Событие `friend_added` появляется в ленте
- [ ] Переводы работают (EN/RU)
- [ ] Навигация "Друзья" ведет на правильный экран

### Edge Cases
- [ ] Нельзя добавить себя в друзья
- [ ] Нельзя отправить дубликат запроса
- [ ] Нельзя принять несуществующий запрос
- [ ] Поиск требует минимум 2 символа
- [ ] Удаление друга работает корректно

---

## 🐛 Известные проблемы / Ограничения

### Текущие ограничения:
- ❌ Нет статусов онлайн/офлайн (Phase 5.1)
- ❌ Нет истории взаимодействий (Phase 5.3)
- ❌ Нет категорий друзей (Phase 5.2)
- ❌ Рекомендации только на основе рефералов (не ML)

### Возможные улучшения:
- Добавить кэширование списка друзей
- Добавить WebSocket уведомления о новых запросах
- Добавить фильтрацию и сортировку друзей
- Добавить групповые действия (добавить несколько друзей)

---

## 📝 Результаты тестирования

**Дата тестирования:** _______________  
**Тестировщик:** _______________  
**Версия:** 1.0

### Результаты:

| Тест | Статус | Комментарии |
|------|--------|-------------|
| Тест 1: Отправка запроса | ⬜ | |
| Тест 2: Принятие запроса | ⬜ | |
| Тест 3: Отклонение запроса | ⬜ | |
| Тест 4: Удаление друга | ⬜ | |
| Тест 5: Поиск пользователей | ⬜ | |
| Тест 6: Рекомендации | ⬜ | |
| Тест 7: Лента активности | ⬜ | |
| Тест 8: Edge Cases | ⬜ | |

**Общий статус:** ⬜ Пройдено / ⬜ Есть ошибки

**Найденные баги:**
1. 
2. 
3. 

---

## 🚀 После успешного тестирования

Если все тесты пройдены:
1. ✅ Система друзей MVP готова к продакшену
2. ✅ Можно переходить к Phase 4.2 (улучшения поиска и рекомендаций)
3. ✅ Можно переходить к Phase 5 (дополнительные функции)

---

**Автор:** AI Assistant  
**Версия:** 1.0  
**Дата:** 2026-02-16
