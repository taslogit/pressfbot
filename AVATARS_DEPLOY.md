# 🖼️ ИНСТРУКЦИЯ ПО ЗАГРУЗКЕ АВАТАРОВ НА СЕРВЕР

## Шаг 1: Подключись к серверу

```bash
ssh user@your-server-ip
```

## Шаг 2: Перейди в директорию проекта

```bash
cd /path/to/pressf
```

## Шаг 3: Создай папку для аватаров (если её нет)

```bash
mkdir -p server/static/avatars
```

## Шаг 4: Загрузи аватары с локального компьютера

### Вариант 1: Через PowerShell скрипт (самый простой)

**С локального компьютера (PowerShell):**

```powershell
# Перейди в папку с аватарами
cd H:\pressf\avatars

# Запусти скрипт (замени на свои данные!)
.\upload_avatars.ps1 -ServerIP "123.45.67.89" -ServerUser "root"
```

### Вариант 2: Через SCP вручную

**ВАЖНО: Замени `user@server` на реальные данные!**

```powershell
# Перейди в папку с аватарами
cd H:\pressf\avatars

# Замени:
# - user → твой пользователь (root, ubuntu, и т.д.)
# - server → IP адрес сервера (123.45.67.89) или домен
# - /path/to/pressf → реальный путь (обычно ~/pressf)

# Пример правильной команды:
scp *.jpg root@123.45.67.89:~/pressf/server/static/avatars/
```

**Или используй WinSCP / FileZilla:**
- Хост: IP сервера
- Пользователь: user
- Путь на сервере: `/path/to/pressf/server/static/avatars/`
- Загрузи все файлы из `H:\pressf\avatars`

### Вариант 2: Через Git (если папка в репозитории)

```bash
# На сервере
cd /path/to/pressf
git pull origin main
```

## Шаг 5: Проверь права доступа

```bash
# На сервере
chmod -R 755 server/static/avatars
```

## Шаг 6: Перезапусти backend контейнер

```bash
docker compose -f docker-compose.traefik.yml restart backend
```

## Шаг 7: Проверь что аватары доступны

```bash
# Проверь что файлы на месте
ls -la server/static/avatars/

# Проверь через API (замени DOMAIN на свой домен)
curl https://your-domain.com/api/avatars
```

---

## Быстрая команда (всё в одной строке)

**С локального компьютера:**

```bash
cd H:\pressf\avatars && scp *.jpg user@server:/path/to/pressf/server/static/avatars/
```

**На сервере:**

```bash
cd /path/to/pressf && chmod -R 755 server/static/avatars && docker compose -f docker-compose.traefik.yml restart backend
```

---

## Проверка

После загрузки открой приложение и перейди в профиль. Должны отображаться все загруженные аватары, а `pressf.jpg` будет дефолтным.
