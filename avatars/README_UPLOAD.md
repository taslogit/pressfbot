# 📤 ЗАГРУЗКА АВАТАРОВ НА СЕРВЕР

## Способ 1: Через PowerShell скрипт (рекомендуется)

1. Открой PowerShell в папке `H:\pressf\avatars`
2. Запусти скрипт с параметрами:

```powershell
.\upload_avatars.ps1 -ServerIP "YOUR_SERVER_IP" -ServerUser "YOUR_USERNAME"
```

**Пример:**
```powershell
.\upload_avatars.ps1 -ServerIP "123.45.67.89" -ServerUser "root"
```

---

## Способ 2: Через SCP вручную

**Замени в команде:**
- `user` → твой пользователь на сервере (например: `root`, `ubuntu`, `ivan`)
- `server` → IP адрес или домен сервера (например: `123.45.67.89` или `pressfbot.ru`)
- `/path/to/pressf` → реальный путь на сервере (обычно `~/pressf` или `/home/user/pressf`)

**Правильная команда:**
```powershell
cd H:\pressf\avatars
scp *.jpg root@123.45.67.89:~/pressf/server/static/avatars/
```

**Или если путь другой:**
```powershell
scp *.jpg root@123.45.67.89:/home/user/pressf/server/static/avatars/
```

---

## Способ 3: Через WinSCP (графический интерфейс)

1. Скачай и установи [WinSCP](https://winscp.net/)
2. Подключись к серверу:
   - **Протокол:** SFTP
   - **Хост:** IP твоего сервера
   - **Пользователь:** твой пользователь
   - **Пароль:** твой пароль
3. Перейди в папку: `/path/to/pressf/server/static/avatars/`
4. Перетащи все файлы из `H:\pressf\avatars` в эту папку

---

## Способ 4: Через FileZilla

1. Скачай [FileZilla](https://filezilla-project.org/)
2. Подключись к серверу (SFTP)
3. Перейди в папку: `/path/to/pressf/server/static/avatars/`
4. Загрузи все файлы

---

## После загрузки (на сервере)

```bash
# 1. Установи права доступа
chmod -R 755 server/static/avatars

# 2. Перезапусти backend
docker compose -f docker-compose.traefik.yml restart backend

# 3. Проверь что файлы на месте
ls -la server/static/avatars/ | head -20
```

---

## Проверка

После загрузки проверь через API:
```bash
curl https://your-domain.com/api/avatars
```

Или открой приложение и перейди в профиль → выбор аватара.
