# 🖼️ НАСТРОЙКА АВАТАРОВ

## Как добавить аватары на сервер

### 1. Подготовка файлов
- Подготовь изображения аватаров (PNG, JPG, GIF, WEBP, SVG)
- Рекомендуемый размер: 256x256 или 512x512 пикселей
- Формат имени файла: `avatar-name.png` (без пробелов, используй дефисы или подчеркивания)

### 2. Загрузка на сервер

#### Вариант 1: Через Docker volume
```bash
# Скопируй файлы в папку на сервере
scp avatar-*.png user@server:/path/to/pressf/server/static/avatars/
```

#### Вариант 2: Через Docker exec
```bash
# Зайди в контейнер
docker exec -it pressf-backend-1 sh

# Скопируй файлы в контейнер
docker cp avatar-1.png pressf-backend-1:/usr/src/app/static/avatars/
docker cp avatar-2.png pressf-backend-1:/usr/src/app/static/avatars/
```

#### Вариант 3: Через volume mount
Если у тебя настроен volume для static файлов, просто скопируй файлы в папку на хосте.

### 3. Структура папки
```
server/
  static/
    avatars/
      avatar-1.png
      avatar-2.jpg
      avatar-3.webp
      ...
```

### 4. Проверка
После загрузки файлов:
- Перезапусти backend контейнер: `docker-compose restart backend`
- Проверь API: `GET /api/avatars` должен вернуть список аватаров
- В приложении открой профиль и нажми на аватар - должен появиться список

### 5. Имена файлов
- Имя файла (без расширения) станет ID аватара
- Например: `cyber-punk.png` → ID: `cyber-punk`
- Отображаемое имя: `Cyber Punk` (автоматически форматируется)

### 6. Поддерживаемые форматы
- PNG (рекомендуется)
- JPG/JPEG
- GIF
- WEBP
- SVG

### 7. Оптимизация
Для лучшей производительности:
- Используй WEBP для меньшего размера
- Оптимизируй изображения перед загрузкой
- Рекомендуемый размер файла: < 100KB

---

**Готово!** Теперь пользователи смогут выбирать аватары из загруженных файлов.
