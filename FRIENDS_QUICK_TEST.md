# ⚡ Быстрый старт тестирования системы друзей

## 🚀 Шаги для начала тестирования

### 1. Применить миграции БД

```bash
# Если используете Docker
docker exec -it pressf-backend-1 node migrations.js

# Или если запускаете напрямую
cd pressf/server
node migrations.js
```

**Проверка:**
```bash
docker exec -it pressf-db-1 psql -U pressf_user -d pressf_db -c "\d friendships"
```

Должна появиться таблица `friendships` со всеми полями.

---

### 2. Перезапустить backend (если нужно)

```bash
docker compose -f docker-compose.yml -f docker-compose.traefik.yml restart backend
```

---

### 3. Пересобрать frontend (если нужно)

```bash
cd pressf/frontend
npm run build
```

---

## ✅ Быстрая проверка что всё работает

### Минимальный тест (2 пользователя):

1. **User A:**
   - Открыть приложение
   - Перейти в "Друзья" → "Поиск"
   - Найти User B (по username или ID)
   - Нажать "Добавить в друзья"
   - ✅ Должен появиться toast "Friend request sent!"

2. **User B:**
   - Открыть приложение
   - Перейти в "Друзья" → "Запросы"
   - В "Входящие запросы" должен быть User A
   - Нажать "Принять" (зеленая галочка)
   - ✅ Должен появиться toast "Friend request accepted!"

3. **Проверка результата:**
   - User A: "Друзья" → "Мои друзья" → должен быть User B
   - User B: "Друзья" → "Мои друзья" → должен быть User A
   - Главная страница → ActivityFeed → вкладка "Friends" → должна быть активность "User A added User B as a friend"

---

## 🐛 Если что-то не работает

### Проверка логов backend:
```bash
docker logs pressf-backend-1 --tail 50 -f
```

### Проверка логов frontend:
- Открыть DevTools (F12)
- Вкладка Console
- Искать ошибки связанные с `/api/friends`

### Проверка что таблица создана:
```sql
SELECT COUNT(*) FROM friendships;
-- Должно вернуть число (может быть 0 если еще нет друзей)
```

### Проверка что маршруты зарегистрированы:
В логах backend при старте не должно быть ошибок про `createFriendsRoutes`.

---

## 📝 Чеклист готовности

Перед тестированием убедитесь:
- [ ] Миграции применены (таблица `friendships` существует)
- [ ] Backend перезапущен
- [ ] Frontend пересобран (или dev server запущен)
- [ ] Нет ошибок в логах backend
- [ ] Нет ошибок в консоли браузера
- [ ] Есть минимум 2 тестовых пользователя

---

**Полный план тестирования:** см. `FRIENDS_TESTING_PLAN.md`
